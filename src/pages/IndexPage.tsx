import React, { memo } from 'react'

const IndexPage = memo(() => {
  return (
    <div>
      <div>这是CoderMilk的代码生成工具，主要生成React，Vue的API or Page UI</div>
    </div>
  )
})

IndexPage.displayName = 'IndexPage'

export default IndexPage
